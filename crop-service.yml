server:
  port: 8084

spring:
  application:
    name: crop-service

  datasource:
    url: jdbc:mysql://localhost:3306/greenhouse_crop_db?createDatabaseIfNotExist=true
    username: root
    password:  Vihanga@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka